{"config.json":"{\"mode\":\"code\",\"github\":null,\"board\":\"openbit\",\"level\":\"Beginner\"}","main.py":"import ESPNOW as now\r\nfrom machine import Pin\r\nfrom time import sleep\r\nimport OLED\r\noled = OLED.SSD1306_I2C(128,64)\r\nsw1 = Pin(2,Pin.IN)\r\nprint(now.getMyMAC())\r\nx = False\r\nOLED = 0\r\ncount = 0\r\nm = False\r\nu = 0\r\n\r\n\r\nwhile True:\r\n    display.show(\"S\")\r\n    if now.isReadyToRead():\r\n        if now.readAsText() == \"One\": #สร้างมาเพื่อรับค่า One ไม่ให้ไปเข้าเงื่อนไขรับค่าในลูป ในกรณีที่รอ stop ตอนที่กำลังเลือกเลขอีกฝั่ง\r\n            sleep(0.2)\r\n    if now.isReadyToRead():\r\n        if now.readAsText() == \"count\":#เก็บค่า count ที่ส่งมาจากอีกฝั่งหลัง\r\n            count += 1\r\n            print(count)\r\n            sleep(0.2)\r\n    swA = sw1()\r\n    if swA == 0:\r\n        now.send(\"Two\",'8C:CE:4E:BB:4A:FC')\r\n        oled.poweron()\r\n        x = True\r\n        sleep(0.2)\r\n        while x:\r\n            swA = sw1()\r\n            if swA == 0:\r\n                OLED += 1\r\n                print(\"OLED : \",OLED)\r\n                oled.text(str(OLED),0,0,1)\r\n                oled.show()\r\n                oled.fill(0)\r\n                sleep(0.2)\r\n            if now.isReadyToRead():\r\n                x = False\r\n                oled.poweroff()\r\n                sum1 = OLED\r\n                print(\"Stop\")\r\n                count += 1\r\n                print(count)\r\n                now.send(\"count1\",'8C:CE:4E:BB:4A:FC')\r\n                sleep(0.2)\r\n            if count == 2:\r\n                m = True\r\n                sleep(0.1)\r\n                while m:\r\n                    now.send(sum1,'8C:CE:4E:BB:4A:FC')\r\n                    sleep(0.3)\r\n                    if now.isReadyToRead():\r\n                        if now.readAsNumber():\r\n                            v = now.readAsNumber()\r\n                            sumall = sum1 + v\r\n                            sleep(0.1)\r\n                            print(\"OUTPUT\",sumall)\r\n                            sleep(0.1)\r\n                            m = False\r\n                            oled.poweron()\r\n                            oled.text(str(sumall),0,0,1);oled.show()\r\n                            OLED = 0\r\n                            sum1 = 0\r\n                            count = 0\r\n    if count > 2:                                           \r\n        count = 0\r\n#หลักการทำงาน\r\n#เมื่อกดสวิต A ครั้งแรก จะส่งค่าที่กำหนดไปให้อีกบอร์ด จากนั้นเข้าลูป x\r\n\r\n#ภายในลูป x จะมีเงื่อนไขการกดสวิตอีกหนึ่งอัน เมื่อกดจะเพิ่มค่าตัวแปร OLED ขึ้น 1(OLED ก็จะเท่ากับ 1)\r\n\r\n#เเละปริ้นโชว์ที่  Terminal และ จอOLED จะกดเพื่อเพิ่มค่าวนไปเรื่อยๆได้ จนกว่าจะกดสวิตอีกฝั่ง\r\n\r\n#เมื่ออีกฝั่งกดสวิต อีกฝั่งจะทำการส่งค่ามา ค่าจะเข้ามาที่เงื่อนไขที่รอรับค่าในลูป จากนั้นจะออกจากลูป x\r\n\r\n#นำ sum1 มาแทนที่ค่า OLED และบวก count หรือลำดับการทำงานขึ้น 1(count ก็จะมีค่าเท่ากับ 1)\r\n\r\n#จากนั้นเข้าสู่การทำงานของอีกบอร์ด กดเลือกเลขอีกบอร์ด และกดสวิตที่บอร์ดที่ไม่ได้เลือกตัวเลขหรือบอร์ดทีรอรับค่าเพื่อตกลง\r\n\r\n#และอีกฝั่งจะส่งค่า count หรือลำดับการทำงานมาอีก 1 จนค่า count ครบ 2\r\n\r\n#ก็จะเข้าเงื่อนไขสุดท้าย เริ่มจากเข้าลูป m ส่งค่า sum1 ไปที่อีกบอร์ด และรอรับค่า sum 2 จากอีกบอร์ดเช่นกัน\r\n\r\n#จากนั้น นำค่า sum2ที่อีกบอร์ดส่งมา มาบวกกับsum1 ที่ตัวเองมีอยู่แล้ว และนำผลบวกไปเก็บในตัวแปรใหม่ ชื่อ sumall \r\n\r\n#จากนั้นปริ้นค่า sumall  ออก Terminal  และออกจากลูป m(m = False) จากนั้นจึงปริ้นค่าออกสู่จอ OLED\r\n\r\n#ละรีค่า OLED sum1 และ count ให้เป็น 0 เพื่อรอการกดสวิต เริ่มนับ1ใหม่อีกครั้ง"}